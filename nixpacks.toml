[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput"
]

[phases.deploy]
cmds = [
  "python manage.py migrate --noinput",
  "python manage.py seed_games"
]

[start]
cmd = "gunicorn gameportal.wsgi --bind 0.0.0.0:$PORT --log-file -"
